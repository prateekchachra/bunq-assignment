import React from 'react';

// TODO: Authorization, pre-commit hook to check types, enforce yarn
// User conservations - If it's currently logged in user, be able to view conversation of the current user
const UserPage = () => {
    return (<div>
        User page
    </div>)
}

export default UserPage;